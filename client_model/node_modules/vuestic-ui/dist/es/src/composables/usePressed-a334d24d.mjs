import { ref } from "vue";
import { u as useHTMLElement } from "./useHTMLElement-af551f75.mjs";
import { u as useEvent } from "./useEvent-0856a587.mjs";
function usePressed(el) {
  const isPressed = ref(false);
  const onMouseDown = () => {
    isPressed.value = true;
  };
  const onMouseUp = () => {
    isPressed.value = false;
  };
  const target = useHTMLElement(el);
  useEvent(["mousedown", "touchstart", "dragstart"], onMouseDown, target);
  useEvent([
    "mouseup",
    "mouseleave",
    "touchend",
    "touchcancel",
    "drop",
    "dragend"
  ], onMouseUp, true);
  return { isPressed, onMouseDown, onMouseUp };
}
export {
  usePressed as u
};
//# sourceMappingURL=usePressed-a334d24d.mjs.map
